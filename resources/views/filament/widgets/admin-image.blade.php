<div class="filament-info-widget p-4 bg-white shadow rounded-lg">
    <div class="flex items-center gap-3">
        <img src="{{ env('APP_IMAGE') }}" alt="{{__('message.company_name')}}" class="h-10 w-10 rounded-full">
        <h1>{{ __('message.company_name') }}</h1>
    </div>
    <p>{{ __('message.company_welcome') }} </p>
</div>
